-- ─────────────────────────────────────────────────────────────────────────────
-- แขวนเมนูตัวเข้าให้ผู้เล่นแต่ละคนตอนพิมพ์คีย์เวิร์ดเข้าร่วมครั้งแรก (BR-78 ฝั่งผู้เล่น)
--
-- publishRichMenus (ตอน "ส่งขึ้น LINE") ลงทะเบียนเมนูไว้บนเซิร์ฟเวอร์ LINE เท่านั้น
-- ไม่เคยผูกให้ผู้เล่นคนไหนเห็นเลย — linkRichMenuBatch ที่มีอยู่แล้วย้ายเฉพาะคนที่
-- ผูกกับเมนูรุ่นเก่าอยู่แล้วตอน republish คนละเรื่องกับคนที่เพิ่งเข้าร่วมครั้งแรก
-- ซึ่งไม่เคยผูกอะไรมาก่อนเลย คอลัมน์นี้บอกว่าผู้เล่นคนนั้นถูกผูกเมนูตัวเข้าไปแล้ว
-- หรือยัง กันไม่ให้ยิง LINE API ซ้ำทุกข้อความ และเป็นจุดที่ mark หลังยิงสำเร็จจริง
-- เท่านั้น (ไม่ใช่ตอน "จะยิง") — ถ้ายิงไป LINE ไม่สำเร็จ ต้องยังลองใหม่ได้ข้อความถัดไป
-- ─────────────────────────────────────────────────────────────────────────────

ALTER TABLE participant ADD COLUMN rich_menu_linked_at TIMESTAMPTZ;
